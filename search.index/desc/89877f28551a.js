rd_("CfPersistence helpers for document metadata and content.CdMap from document id string to <code>DocMeta</code>.CnFilename where the document content is stored (relative to \xe2\x80\xa6AoReturns the argument unchanged.0000BaCalls <code>U::from(self)</code>.0000BeHuman-readable title of the document.BfDocument title provided by the client.CmUtilities to load site configuration values from files in \xe2\x80\xa6CeData models and request DTOs used by the backend API.ClReturn the current system time in milliseconds since the \xe2\x80\xa6CeMetadata for a single document stored by the service.CkContainer mapping document IDs to metadata and the next \xe2\x80\xa6BjRaw markdown content for the new document.CiWhether the document has been deleted (soft-delete flag).BiHandler to fetch a single document by id.CgNext numeric id to assign when creating a new document.ChShared application state accessible by request handlers.CcDirectory where document markdown files are stored.CcCollection of HTTP request handlers for API routes.CcHandler that returns a list of available documents.DjDirectory where configuration files such as <code>site.json</code> and \xe2\x80\xa6CdCreation timestamp in milliseconds since UNIX epoch.CdHandler to mark a document as deleted (soft delete).C`Whether the document is marked as deprecated \xe2\x80\xa6Dc<code>true</code> to mark deprecated, <code>false</code> to unmark.CbStart and run the HTTP server for the application.CeHandler to create a new document from a JSON payload.BlRequest payload for creating a new document.BhReturn the admin sequence configuration.CePayload used to mark a document as deprecated or not.DbCheck whether the provided <code>X-Session-Token</code> header \xe2\x80\xa6EoLoad the <code>DocsMap</code> structure from <code>docs.json</code> in <code>config_dir</code>.DoPersist the provided <code>DocsMap</code> into <code>docs.json</code> under \xe2\x80\xa6CiCreate a new admin session token and return it to the \xe2\x80\xa6EbLoad the site name from <code>site.json</code> located in <code>config_dir</code>.CiHandler to mark a document as deprecated or to unmark it.DgDestroy the session token provided via <code>X-Session-Token</code> \xe2\x80\xa6EcRead the raw markdown content for <code>filename</code> from <code>docs_dir</code>.EnSave raw markdown <code>content</code> into <code>filename</code> under <code>docs_dir</code>.EcLoad the admin sequence from <code>sequence.json</code> in <code>config_dir</code>.C`Backend framework for the Ducia listing service.")